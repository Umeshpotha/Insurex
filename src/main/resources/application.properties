spring.application.name=Insurex

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/insurex
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate (JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Optional
spring.main.banner-mode=off

# Override these with environment-specific values outside local development.
insurex.admin.email=${INSUREX_ADMIN_EMAIL:admin@insurex.com}
insurex.admin.password=${INSUREX_ADMIN_PASSWORD:admin123}
